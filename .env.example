# Copy this file to .env and fill in your workshop credentials.
# .env is gitignored — never commit real credentials.
#
# Your credentials are available on the workshop credentials page
# (QR code / URL provided at the start of the session).

SPLUNK_ACCESS_TOKEN=your-access-token-here
SPLUNK_REALM=us1
PARTICIPANT_ID=your.email@example.com

# Take-home Exercise 1 only — add your GitHub username
GITHUB_USERNAME=your-github-username-here
